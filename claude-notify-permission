#!/usr/bin/env fish

source /etc/nixos/claude-notify.fish

set window_pid (find_ancestor_window)
if test -n "$window_pid"; and is_focused_window $window_pid
    exit 0
end

# Read hook input from stdin.
set input (cat)

set tool (echo $input | jq -r '.tool_name')
set -l xml_escape 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g; s/\\\\/\\&#92;/g'
if test "$tool" = Bash
    set description (echo $input | jq -r '.tool_input.description // empty' | sed $xml_escape | string collect)
    set command (echo $input | jq -r '.tool_input.command // empty' | sed 's/ && / \\\\\n  \&\& /g; s/ || / \\\\\n  || /g; s/ | / \\\\\n  | /g' | shfmt -bn -i 2 - | sed $xml_escape | string collect)
    set body "<b>$description</b>
<span font_family=\"monospace\">$command</span>"
else if test "$tool" = Write
    set file_path (echo $input | jq -r '.tool_input.file_path // empty')
    set content (echo $input | jq -r '.tool_input.content // empty' | sed $xml_escape | string collect)
    set body "<b>Write: $file_path</b>
<span font_family=\"monospace\">$content</span>"
else if test "$tool" = Edit
    set file_path (echo $input | jq -r '.tool_input.file_path // empty')
    set replace_all (echo $input | jq -r '.tool_input.replace_all // empty')
    set old_string (echo $input | jq -r '.tool_input.old_string // empty' | sed $xml_escape | string collect)
    set new_string (echo $input | jq -r '.tool_input.new_string // empty' | sed $xml_escape | string collect)
    set header "<b>Edit: $file_path</b>"
    if test "$replace_all" = true
        set header "$header (replace all)"
    end
    set body "$header

<b>old:</b>
<span font_family=\"monospace\">$old_string</span>

<b>new:</b>
<span font_family=\"monospace\">$new_string</span>"
else
    set detail (echo $input | jq '.tool_input' | sed $xml_escape | string collect)
    set body "<b>$tool</b>
<span font_family=\"monospace\">$detail</span>"
end

set -l actions -A allow=Allow
if test "$tool" = Bash
    set -l program (echo $input | jq -r '.tool_input.command' | string split -m 1 ' ')[1]
    set -a actions -A "allow-always:$program=Allow Always"
end
set -a actions -A deny=Deny -A default=Focus

set action (send_notification --window-pid "$window_pid" \
    --focus-on deny --focus-on default \
    $actions -- "$body")

if test "$action" = allow; or string match -q 'allow-always:*' -- "$action"
    echo '{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}'
    if string match -q 'allow-always:*' -- "$action"; and test -n "$window_pid"
        set -l program (string replace 'allow-always:' '' -- "$action")
        swaymsg exec -- "alacritty --config-file /etc/nixos/alacritty.toml -e fish -c 'cd /etc/nixos; claude --worktree -- \"/add-rule $program\"'"
    end
else if test "$action" = deny
    echo '{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"deny","message":"Rejected via notification"}}}'
end
